#pragma once

/**
 * @brief Calculates the geometric mean of a dataset.
 * @note Input vector must not contain negative values.
 * @param data A vector of doubles representing the source dataset.
 * @return The calculated geometric mean as a double.
 * @throws std::invalid_argument If the dataset is empty.
 */
class generator {
public:
	//static int generate();
};